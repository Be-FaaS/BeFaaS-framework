config:
  target: ' '
  phases:
    - duration: 1
      arrivalRate: 2
    - duration: 3
      arrivalRate: 1
  processor: '../../artillery/logger.js'
  defaults:

scenarios:
  - flow:
      - get:
          url: '{{ frontend }}'
          beforeRequest: 'beforeRequest'
          afterResponse: 'afterResponse'
    name: 'frontend-frontpage'
