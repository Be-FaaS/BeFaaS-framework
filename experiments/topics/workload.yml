# Constant workload for the topics FaaS benchmark.
config:
  target: ' '
  phases:
    #Constant load for 15min (900s), 3.5 flows per second
    - duration: 100
      arrivalRate: 3.5
  processor: '{{ $processEnvironment.PROCESSOR_DIR }}/logger.js'
  defaults:

scenarios:
  # Just call awsfunction1
  - flow:
      - post:
          url: '{{ function1aws }}/call'
          json:
              pudding:
                name: 'Schokopudding'
                zucker: 4
          beforeRequest: 'beforeRequest'
          afterResponse: 'afterResponse'
    name: 'just function1aws'
    weight: 100