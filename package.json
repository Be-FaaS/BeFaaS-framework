{
  "name": "experiments",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "standard",
    "fix": "standard --fix",
    "local": "node -e \"http.createServer(require(process.argv[1]).googleHandler).listen(3000)\"",
    "build": "./scripts/build.sh",
    "deploy": "./scripts/deploy.sh"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@faastermetrics/lib": "^3.0.0",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "@zeit/ncc": "^0.22.1",
    "standard": "^14.3.3"
  }
}
